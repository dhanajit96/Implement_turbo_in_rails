<!DOCTYPE html>
<html>
  <head>
    <title>Spa</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      .suggestions-box {
        border-verticle: 1px solid #ccc;
        background: white;
        position: absolute;
        z-index: 1000;
        width: 300px;
      }

      .suggestion {
        padding: 4px 6px;
        cursor: pointer;
      }

      .suggestion:hover {
        background-color: #f0f0f0;
      }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
